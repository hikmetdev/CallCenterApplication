# Proje Ad? (opsiyonel)
spring.application.name=callcenter-api

# PostgreSQL ba?lant?s?
spring.datasource.url=jdbc:postgresql://192.168.234.90/callcenter_db 
spring.datasource.username=postgres
spring.datasource.password=default123
# farklı bir kullanıcıdan bağlandığında ana makinanın ip yazılacak.

# JPA / Hibernate ayarlar?
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Port (opsiyonel)
server.port=8080

spring.jpa.properties.hibernate.format_sql=true

server.address=0.0.0.0
